layout {
    default_tab_template {
        // The main pane where your terminal/code goes
        pane
        
        // The single bar at the bottom, mimicking Tmux
        pane size=1 borderless=true {
            plugin location="zellij:compact-bar"
        }
    }
}
