# wget user config. WGETRC points here (set in xdg-env.sh / xdg.fish).
# wget has no dedicated env var for the HSTS database, so relocate it here.
# wget tilde-expands this path, so it stays portable across machines.
hsts-file = ~/.local/state/wget/wget-hsts
