# editline configuration

# use vi mode
bind -v

# use jk in normal mode to do history completion
bind -a "k" ed-prev-history
bind -a "j" ed-next-history

# tab completion for lldb (maybe not the best way to handle this)
# bind "\t" lldb-complete
